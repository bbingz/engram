// Stage 2 adapter contracts intentionally reuse the shared JSONValue defined in
// Shared/MCP/MCPProtocol.swift so app, MCP, CLI, and CoreRead targets compare
// parity fields with a single JSON representation.
