{
  "content": [
    {
      "type": "text",
      "text": "Search mode 'semantic' is unavailable; session embeddings are not usable. Configure an embedding provider and wait for semantic_chunks backfill, or use mode 'keyword'."
    }
  ],
  "isError": true,
  "structuredContent": {
    "code": "searchModeUnavailable",
    "message": "Search mode 'semantic' is unavailable; session embeddings are not usable. Configure an embedding provider and wait for semantic_chunks backfill, or use mode 'keyword'."
  }
}
