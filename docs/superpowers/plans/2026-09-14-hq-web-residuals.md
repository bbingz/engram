# HQ Web residuals — Cursor dispatch (2026-09-14)

Generated by Grok workflow `engram-next-backlog` (history then plan).
Dispatcher: Herdr `wH:p4` grok. Implementer: Herdr `wH:p2` cursor.

## Next work: HQ Web residuals (collector worktree)

**Workdir:** `/Users/bing/-Code-/engram/.worktrees/collector-server-web-20260905`  
**Branch:** `codex/collector-server-web-20260905` @ `922a47b6` (not merged; main is `625ecc97`)  
**Live HQ:** service-index `web-parity-20260913-r17`; remote-server still `r5`. Do not activate a new package.

`docs/TODO.md` Open is empty. Roadmap Decision pending rows stay parked. `live-ingest-sla-1` is ops, not this pass.

| Pri | ID | Title | Done when |
|-----|----|-------|-----------|
| p0 | `overview-default-limit-50-503` | Omitted `/web/api/overview` limit 50 → 17-stream 2.85s/503 | Default limit is **2** (UI page size); HTTP/DTO tests pin omitted=2; no readyCount rewrite |
| p0 | `cjk-two-scalar-sessions-list-503` | `query=测试` list filter LIKE-scans 777k FTS rows past 2s | Metadata path never unbounded-LIKE; typed short-query + UI; MATCH ≥3 scalars unchanged |
| p1 | `cjk-two-scalar-search-5-6s` | Search LIKE 5–6s will miss 8s as corpus grows | Bounded sub-trigram plan (fts_map early-stop or short index) with fixture `_repro` |
| p1 | `files-agents-all-1.14s` | Files `agents=all` heap-reads 11.8k via non-covering activity index | Idempotent covering partial + statistics-free EXPLAIN; no HQ migrate |
| p1 | `health-parsed-skip-terminal` | Parsed 28k vs Index ready 1.3k looks like backlog; all skip | Relabel/split; **do not** FTS-index skip |
| p2 | `health-cursor-quarantine-misread` | Cursor 56–57 quarantines are `parse.noVisibleMessages` | Shared empty-transcript copy; no Cursor parser hunt |

**Do not:** merge to main, v1.0.6, Docker, write `~/.engram`, ANALYZE HQ, promote skip → `index_ready`, revert r9–r17, touch sibling worktrees.
---

## Implementation brief

You are implementing the next HQ native-Web residuals in the Engram collector worktree. Read-write for this worktree only.

Workdir (must use this checkout, not HQ main):
  /Users/bing/-Code-/engram/.worktrees/collector-server-web-20260905
Branch: codex/collector-server-web-20260905
HEAD to start from: 922a47b6c9f0e39efef271b8e3288088f4b368da (same as origin/codex/collector-server-web-20260905).
Main is 625ecc9737c219f401200d3c2e301f537582ff11 and must not receive this work.

Product context: HQ native Web is already live at https://macmini-hq.tail1cb16.ts.net:8443/web/ with service-index web-parity-20260913-r17 (EngramServiceCore 369f3ddd…5d83d) and remote-server still r5. r9–r17 already shipped ByteKey, children unary-plus, session_files CROSS JOIN backfill, file-activity unsorted/page-only redaction, and INDEXED BY idx_sessions_activity_time for agents=all/only. Do not reopen those. Plan file: docs/superpowers/plans/2026-09-13-web-native-parity.md (worktree only). Residuals: CHANGELOG.md top entries + plan lines 401–464.

Hard constraints:
- Do not merge into main. FETCH_HEAD already marks this branch not-for-merge.
- No public v1.0.6, tag, GitHub Release, Homebrew, Sparkle.
- No Docker.
- No production ~/.engram writes: no live migrate, no ANALYZE / PRAGMA optimize on HQ index.sqlite, no package activate, no launchctl. Tests use isolated fixtures / tmp DBs only. Statistics-free fixtures (no sqlite_stat1) like HQ.
- Do not promote skip-tier sessions to index_ready or create FTS jobs for skip.
- Do not turn roadmap Decision pending rows into work.
- Do not git reset --hard or revert unrelated dirt. If the worktree is dirty, keep it; only add this task’s diffs.
- Minimum diff. Swift product is source of truth. After new Swift files: cd macos && xcodegen generate. No macos/build/. No Node product startup.
- Comments in English; Chinese comments already in the tree stay.

Implement in this order. Stop after each task’s tests are green before starting the next. Cap is these six tasks.

Task 1 — p0 overview-default-limit-50-503
Omitted GET /web/api/overview currently does `fields["limit"] ?? 50` in macos/EngramRemoteServer/Core/WebReadRoutes.swift (overviewRequest) and EngramServiceWebOverviewRequest(limit: Int = 50) in macos/Shared/Service/EngramServiceWebReadModels.swift. HQ has 17 streams; limit=50 computes all of them (2.85s/503). The browser already uses limit=2 over nine pages (WebUIRoutes.swift loadOverview URLSearchParams({ limit: "2" }); tests/scripts/collector-web-ui.test.ts).
Change the omitted-limit default to 2 (same as the UI). Keep pageRequest(limit in 1...100). Do not rewrite overview readyCount/ledger SQL in this task.
Update tests that pin 50: testOverviewDefaultQueryReturnsTypedDTO (WebMetadataHTTPTests.swift expects recorders.overview limit [50]), testDefaultRequestConstructorsAndInclusivePageLimits (WebReadClientTests.swift XCTAssertEqual(overview.limit, 50)). Add a _repro that omitted HTTP limit is 2. Producer tests that call EngramServiceWebOverviewRequest() with 1 stream should still pass.

Task 2 — p0 cjk-two-scalar-sessions-list-503
CJKText.usesTrigramMatch requires unicodeScalars.count >= 3 (macos/Shared/EngramCore/CJKTextHelpers.swift). ServiceWebMetadataProducer.sessionFilter for !usesTrigramMatch appends `s.id COLLATE BINARY IN (SELECT session_id FROM sessions_fts WHERE content LIKE ?)` (around line 2701). That scan is 5–6s and 503s the 2s metadata deadline (ServiceWebMetadataLimits.maximumRequestDuration = 2s).
Do not run an unbounded sessions_fts LIKE content scan on the metadata path. Preferred: typed short-query contract — empty page + explicit reason, UI copy that 1–2 character filters belong on Search (8s). Do not lower the trigram threshold; do not swap in instr/GLOB; do not ANALYZE. 3+ scalar MATCH / identity-first search stays. RED/GREEN _repro with a statistics-free fixture tracing SQL. Update collector-web-ui.test.ts if the sessions filter UI changes.

Task 3 — p1 cjk-two-scalar-search-5-6s
EngramServiceReadProvider.keywordSearch still MATERIALIZEs mN_hits with content LIKE for sub-trigram tokens (around lines 1232–1240). Keep MATCH for 3+ scalars (testSQLiteReadProviderCJKTrigramTermsRankByRelevance_repro). Add a bounded plan: recency-ordered early-stop through fts_map (currently 1:1 with sessions_fts) or a short-term index with migration tests. Prove with a fixture-scale _repro, not production HQ. Do not claim live 5–6s numbers without measuring a copy you created; do not write ~/.engram.

Task 4 — p1 files-agents-all-1.14s
fileActivityRows joins session_files to sessions via sessionsJoinSQL which INDEXED BYs idx_sessions_activity_time for .all/.only (non-covering: hidden_at, COALESCE(end_time,start_time) DESC WHERE hidden_at IS NULL AND tier not skip). Add an idempotent covering partial in macos/EngramCoreWrite/Database/EngramMigrations.swift and MigrationRunnerTests EXPLAIN on a statistics-free fixture. Keep .hide on idx_sessions_web_list_keys. Do not break testAgentsAllPinsSessionsToSkipExcludingIndex_repro or testFileActivityStatementIsUnsortedAndAuthorityIgnoresRowOrder_repro. Do not activate on HQ.

Task 5 — p1 health-parsed-skip-terminal
WebUIRoutes.swift Health card maps ["Parsed","parsed"] and ["Index ready","indexReady"]. Relabel or split so skip-terminal parsed is not a backlog. Do not change SessionSnapshotWriter.ensureCurrentCaptureFTSJob `guard tier != .skip else { return nil }`. Update collector-web-ui.test.ts Health render (native Health page describe).

Task 6 — p2 health-cursor-quarantine-misread
Same Health card: quarantined counts are parse.noVisibleMessages (empty transcript) HQ-wide, not a Cursor-only parser bug. Add class copy. No Cursor adapter change unless you first add a failing parser _repro (not claimed).

Verifiers (run from workdir; no Docker):
  cd macos && xcodegen generate   # only if you add/remove Swift files
  xcodebuild test -project Engram.xcodeproj -scheme EngramRemoteServerCoreTests -destination 'platform=macOS' CODE_SIGNING_ALLOWED=NO
  xcodebuild test -project Engram.xcodeproj -scheme EngramServiceCore -destination 'platform=macOS' CODE_SIGNING_ALLOWED=NO
  xcodebuild test -project Engram.xcodeproj -scheme EngramCoreTests -destination 'platform=macOS'   # required if Task 4 migration lands
  npm test -- tests/scripts/collector-web-ui.test.ts   # Tasks 2, 5, 6 UI
  npm run lint   # if you touch tests/scripts/*.ts
Use isolated DerivedData under /tmp if the machine is busy; do not write macos/build/.

Done when the six tasks’ tests are green in this worktree. Record what you changed in CHANGELOG.md (English) and a short MEMO.md pointer. Do not push, do not open a PR into main, do not deploy.