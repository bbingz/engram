You are an independent senior reviewer (macOS / SwiftUI / SQLite-FTS5 / IPC / release-engineering expert). A multi-agent review of the Engram macOS app (native SwiftUI app + Swift EngramService helper over a Unix socket + EngramMCP stdio helper; TypeScript is dev/reference only; SQLite at ~/.engram/index.sqlite with FTS5 trigram + intended sqlite-vec) produced the findings below.

You likely do NOT have file access in this mode. So:
- For each finding: judge PLAUSIBLE / DOUBTFUL / NEEDS-FILE-CHECK and explain WHY from architecture/experience. Call out any claim that sounds internally contradictory or like a misread.
- MOST IMPORTANTLY: from your expertise, what is MISSING from this review? What bug classes for an app of this shape (SwiftUI main-thread/data-flow, GRDB/SQLite concurrency + WAL + FTS5 external vs standalone content, Unix-socket IPC framing, macOS sandbox/codesign/notarization, multi-source parsing) are NOT covered and should be?
- Give your own top-5 most severe issues (from the list or your own) and any over-reach you suspect.
Be terse and specific.

## Already empirically verified by the lead reviewer (high confidence)
- V1: current Swift code never writes FTS *content*. sessions_fts is a STANDALONE fts5 table (not external-content, no triggers). Only INSERT is VALUES('optimize'). SessionSnapshotWriter only DELETEs FTS rows + enqueues session_index_jobs(job_kind='fts'); the consumer protocol StartupIndexJobRunning has NO production conformer. Live DB: newest sessions have NO FTS content; 392 non-skip sessions lack FTS content; 137k job rows accumulated. Reader uses INNER JOIN sessions_fts ⇒ new sessions unsearchable by keyword. The 82,931 existing FTS rows were written by an OLD TS/Node runtime.
- V2: EngramServiceRunner.run() (the only thing main.swift calls) only calls writer.indexRecentSessions(). It NEVER calls migrate() (only the CLI EngramCoreSchemaTool does) nor StartupBackfills.runInitialScan (defined, never called). So the documented startup backfill chain, FTS job drain, reconcileInsights, vacuum, orphan scan are dead in production. Inline adapter-time parent detection still works for new sessions.
- V3: fresh-machine fake success. Because migrate() is never called, on a clean ~/.engram the first indexRecentSessions should fail with "no such table: sessions", BUT the per-snapshot catch swallows the error AND SwiftIndexer.indexAll still does indexed += batch.count → reports fake non-zero count with zero writes → permanently empty DB on new install.

## Findings to adjudicate (condensed)
SINGLE SOURCE OF TRUTH (all judged behavioral): system-injection prefix list copy-pasted across 8 sites and diverging (canonical SystemMessageClassifier + 7 inline copies); Codex header injection set (4 tags) narrower than its TS reference (10) despite "mirrors TS" comment; isProviderReviewPrompt/Summary duplicated verbatim with over-broad substring matching (contains("tests ")/"review"/"correctness") that can misclassify a real review session as a throwaway probe → tier=skip → hidden; parent scoring duplicated with DIFFERENT time constants (4h half-life/4h gap vs 6h/48h/30min gap) → live vs backfill disagree on links; parseSessionInfo header counts vs streamMessages diverge in ≥6 adapters (VsCode hardcodes messageCount=requests*2); no parity assertion messageCount==messages.count; no Swift SessionTier test at all; TS tier rules (PROBE_FIRST_LINES, messageCount<=3 probe→lite, 6 NOISE_PATTERNS) absent from Swift (Swift has 2).

ADVERTISED BUT INERT: local semantic search (FTS+sqlite-vec+RRF) not implemented (SQLiteVecSupport.probe returns "not implemented yet"; no Swift embedding client; RRF computes 1/(60+rank) over a single keyword list = no-op; save_insight always text-only); whole AI Settings section (provider/prompt/embeddings×3/title/auto-summary) persisted but never read; generate_summary and regenerateAllTitles are template/extractive (ignore AI settings) yet described as "AI summary"; Layer-3 manual link/unlink dead (request types defined, no handler/client/caller); Windsurf+Antigravity adapters enableLiveSync:false everywhere → zero ingest; "JSON" transcript tab byte-identical to "Text"; hygiene returns constant score=100; CLAUDE.md says web UI removed but it's still wired.

SECURITY (confirmed reachable on the running machine): EngramWebUIServer always-on 127.0.0.1:3457, no auth/CSRF/Origin (curl 200 even with spoofed Host/Origin → DNS-rebinding), serves UNREDACTED transcripts (export path redacts, web does not); project_move/archive/batch src/dst unconfined (no allow-list unlike linkSessions), force:true bypasses git guard; no authz on mutating commands, EngramServiceError.unauthorized is dead code, no getpeereid on accept(); isUnsignedBuild writes API keys plaintext to settings.json (no UI warning) for DerivedData/ad-hoc builds.

SERVICE/IPC: accept() loop treats any accept()<0 as fatal break (EINTR/ECONNABORTED/EMFILE permanently kill the acceptor while process stays alive holding the lock); writeFrame doesn't enforce the 256KB cap readFrame enforces, and search snippet is full untruncated FTS content → limit=100 responses exceed 256KB → client rejects legit results; decode/handler errors reply requestId:"unknown" → client id-match guard masks the real error.

WRITE PATH: backfillSuggestedParents 24h window compares ISO8601 "...T...Z" lexicographically against datetime(?, '-24 hours') (space-separated) → broken lower bound; executeAndCountChanges uses sqlite3_total_changes (trigger-inclusive) → inflated dedup counts; cascade trigger resets tier only for confirmed children not suggested_parent_id children; reconcileInsights id NOT IN (SELECT id FROM insights) soft-deletes the whole vector store when insights is empty/partial, NULL-unsafe.

READ/ADAPTERS: AntigravityAdapter.inferredCWD hardcodes the author's machine layout /Users/<user>/-Code-/<project>; CascadeDiscovery calls waitUntilExit() before readDataToEndOfFile() → pipe-buffer deadlock on large ps/lsof; WatchPathRules maxDrainBatchSize loaded from unrelated key startupParentBackfillLimit.

OBSERVABILITY/UI: 5 Observability views read logs/traces/metrics tables Swift never writes (os_log only) → perpetually empty "all clear"; status command always returns .running, app apply(event:) has no index_error branch → indexing failure invisible; 12 views call synchronous DatabaseManager reads on the main thread (no Task.detached) → UI hangs on a real DB; a11y is test-satisfying (112 identifiers, 5 labels, 0 values; charts invisible to VoiceOver).

RELEASE: export fallback ditto-copies an Apple-Development-signed app and reports "Build complete!" though it cannot be notarized; the only release test asserts script TEXT not behavior; no bundle-hygiene gate; no Hardened Runtime; static uncoordinated versions; --deep verify removed.

## Output: (1) V1/V2/V3 plausibility verdict + any logical hole you see; (2) per-section: only items you'd challenge or strengthen; (3) OMISSIONS (bug classes/areas this review missed); (4) your top-5 by severity; (5) suspected over-reach.
