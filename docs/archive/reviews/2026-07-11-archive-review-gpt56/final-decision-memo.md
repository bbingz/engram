=== SECURITY REVIEW [needs-fixes] ===
(blocker) Internal contradiction: the recovery phrase is the master unwrap secret, yet G1 routes it through a cloud-synced password manager (iCloud Keychain / 1Password) to 'distribute' it to every machine. This makes EVERY one of the user's Macs a full key-holder and moves the confidentiality of the entire archive onto the security of the Apple/1Password cloud account. This directly contradicts G4's threat table, which rates 'Tailnet device compromise (non-key-holder)' as '~ metadata only' — the user's own laptops are the highest-risk tailnet devices and they are all key-holders, not non-key-holders. It also contradicts the doc's own risk calculus: it dismisses home-mini live-root compromise as 'low probability' to justify scheme (c), while simultaneously exposing the master secret to Apple-account / password-manager-account phishing, a strictly higher-probability remote vector. Once any one Mac or the cloud vault is compromised, id_key + enc_key leak and the server's stored HMAC ids become retroactively confirmable, nullifying the per-user-convergence-secret protection of scheme (a) as well.
  FIX: State the confidentiality boundary honestly as 'any device holding the phrase or an unwrapped Keychain key,' and count those devices. If the threat model includes remote adversaries, do NOT sync the master phrase through a cloud password manager: keep the 24-word phrase strictly out-of-band (paper/hardware) and provision each new machine by one-time manual entry, so cloud-account compromise cannot yield the master secret. Redraw the G4 table so the 'tailnet device compromise' row is split into key-holder (=✗ full plaintext) vs non-key-holder (=~), and acknowledge the user's laptops are in the ✗ column.

(blocker) Redaction enforcement point is unspecified per tier, and the verbatim:true bypass is a 'default toggle' rather than an authorization boundary — so remote/cross-machine egress can receive unredacted or verbatim plaintext. Under default scheme (c) the SERVER holds the at-rest key and decrypts bodies; if the redaction filter is implemented only in the Mac app's read resolver, a remote MCP client that queries the server directly bypasses redaction entirely. Under scheme (a) the server cannot decrypt, so redaction must run on the key-holder. The enforcement point therefore MOVES with the tier, but the design never pins it. Separately, 'verbatim:true bypasses the filter' plus 'remaining_user_choice: whether verbatim defaults on for remote MCP' means a remote caller can request raw bytes — turning the entire redaction layer off with one flag.
  FIX: Pin the redaction enforcement point per tier: for (c) redaction MUST run server-side (the only place with plaintext on the remote path); for (a) it runs on the key-holder before any egress. Make verbatim an origin-gated authorization decision, not a default: hard-deny verbatim:true for any non-local-origin request (remote MCP, other tailnet node) regardless of settings; honor it only for the local app on the same host. Add a test that a remote MCP client cannot obtain unredacted bodies via any read path including verbatim.

(major) Scheme (c) — the recommended default for the home mini — protects ONLY against theft of a powered-OFF box. FDE decrypts at boot and the ENGRAM_REMOTE_AT_REST_KEY is server-held, so against every ONLINE adversary (root compromise, RCE in the Engram server, a compromised or over-authorized tailnet peer, a stolen tailnet node key) scheme (c) yields full plaintext — identical to running no app-layer encryption at all. The prose ('removes all key-management burden', 'low probability') materially undersells that (c) adds essentially nothing over plain FileVault/LUKS for any live threat. Compounding this: the doc implies tailnet membership ≈ trust ('every device you own is on the tailnet'). If the server authorizes reads by peer/tailnet-membership rather than requiring the bearer token on every call, then one over-broad ACL entry or one stolen tailnet device key = full plaintext.
  FIX: State the (c) residual bluntly: 'at-rest encryption on the mini protects only against powered-off physical theft; any live compromise or authorized-but-untrusted tailnet reader gets full plaintext.' Require the transport bearer token on EVERY read path as defense-in-depth even inside the tailnet, and never treat tailnet membership as authorization. Constrain which tailnet nodes can even reach the service via Tailscale ACLs, and enable tailnet lock (TKA) so the coordination server cannot unilaterally add a trusted node.

(major) The threat table omits the two most common real-world Mac-loss cases and thereby overstates protection. It lists only 'Stolen/lost Mac (FileVault, off) = ✓'. But (a) a Mac stolen while sleeping/logged-in and (b) malware running as the user both read the operational master_data_key straight from Keychain (WhenUnlocked class is accessible whenever the device is unlocked) and decrypt everything — the scheme provides ZERO protection in these cases because the app itself can decrypt. These are far more probable than powered-off theft.
  FIX: Add explicit rows for 'Mac stolen while unlocked/sleeping' and 'malware as the logged-in user' with an honest ✗, and note the only mitigations there are OS-level (FileVault + short auto-lock + require-password-on-wake + not caching the key across lock), not this crypto chain. Consider re-deriving the operational key from the phrase per session (or gating Keychain access behind a biometric/ACL LAContext) instead of leaving a raw 256-bit master key in WhenUnlocked Keychain indefinitely.

(major) The 24-word recovery phrase is a hard, un-redundant durability SPOF, and the doc's 'two independent factors' framing is misleading. Because the wrapped blob is deliberately replicated to the server and every backup (low-trust locations that the threat model itself treats as potentially compromised), the confidentiality of the WHOLE archive against server-compromise / leaked-backup collapses to a SINGLE factor: scrypt(phrase). Conversely for durability, losing the phrase after Mac loss makes the entire replicated corpus permanently unrecoverable ciphertext even though every byte still exists. There is no threshold/redundancy option offered for the one secret the whole system hangs on.
  FIX: Drop the 'two independent factors' language for the server/backup-compromise case — against those attackers it is one factor (the phrase). Offer at least one durability redundancy that is NOT escrow-under-the-data-key: e.g., a second wrapped copy of master_data_key under a hardware-security-key/passkey KEK, or optional Shamir (2-of-3) split of the phrase across paper + hardware token + trusted location. Specify and pin the scrypt cost parameters (N,r,p) explicitly, and enforce that the enable flow generates the full 24-word phrase — never allow a user-chosen memorable passphrase substitute, since that would reduce whole-archive confidentiality to scrypt(weak-passphrase).

(major) Scheme (a)'s guarantee 'server cannot confirm files' is weaker than claimed because the design does not explicitly adopt a per-repo secret chunk_seed for the content-defined chunker. The cited Borg evidence uses chunk_seed specifically 'to prevent chunk size based fingerprinting attacks,' but the G4 recommendation only carries the keyed HMAC id — not the secret CDC seed. A running-root server in (a) still sees chunk-size sequences, boundary patterns, and within-corpus dedup-equality; without a secret seed those size sequences can fingerprint known files even without confirming content, so the '~ cannot confirm files' cell is optimistic.
  FIX: Explicitly require a per-repo secret chunk_seed (derived from master_data_key via HKDF) driving the CDC rolling hash, matching Borg. Downgrade the (a) server-compromise claim to acknowledge residual metadata leakage: chunk-size fingerprints, access timing, corpus size, and within-corpus repetition counts remain visible even with keyed ids.

(minor) AES-256-GCM nonce derivation is unspecified across a single-enc_key, deduplicated, content-addressed corpus. GCM nonce reuse across two DISTINCT plaintexts under the same key is catastrophic (plaintext XOR recovery + forgery). Convergent dedup deliberately reuses key+nonce for IDENTICAL plaintext (acceptable), but the scheme must guarantee distinct chunks never collide on nonce, and must avoid any random-96-bit-nonce path that would both break dedup and risk birthday-bound reuse over the corpus lifetime. GCM is also not key-committing.
  FIX: Specify deterministic nonce = truncate/KDF(chunk HMAC id) so identical plaintext dedups and distinct chunks get distinct nonces (collision ≡ HMAC collision, negligible); forbid any random-nonce encryption path. If cross-key confusion is a concern, add a key-commitment (e.g., encrypt-then-bind enc_key, or use an AEAD like AES-GCM-SIV / XChaCha20-Poly1305 with a committing construction).

(minor) Egress regex/entropy redaction is presented as satisfying a 'redaction capability before durable store' release gate, but by construction it is a best-effort courtesy filter, not a security boundary — and the canonical store is verbatim ciphertext. D4 deliberately narrows to high-precision structured tokens and excludes prose secrets, so 'the db password is hunter2', OAuth refresh tokens without known prefixes, private keys in non-PEM encodings, and session cookies all pass through to any egress consumer whose redaction path has a gap. The real secret boundary is encryption + key custody, not redaction.
  FIX: Reframe the doc: egress redaction is display hygiene with known, accepted gaps — NOT a guarantee that secrets don't leave the machine. The release gate's actual security property is 'encryption-at-rest + key custody keep secrets off untrusted hardware; egress redaction reduces incidental exposure on trusted-but-shared surfaces.' Keep the narrow high-precision ruleset (the false-positive argument is correct) but stop claiming it as a protection boundary.

(minor) D5's guarantee 'excluded NDA content never enters the durable store' is only as strong as project/cwd attribution. Sessions that span multiple cwds, have a missing/ambiguous cwd, or are misattributed can slip NDA content into a non-excluded project's durable, replicated, backed-up store — where egress redaction (which excludes broad PII by design) will not scrub it.
  FIX: Fail closed on ambiguous or missing cwd (quarantine or treat as excluded pending user classification), match path-glob exclusions against ANY cwd a session touched (not just its primary project id), and add a test for multi-cwd sessions.

(minor) Migrating from nginx + private-CA to Tailscale Serve managed TLS silently drops mTLS client-authentication in favor of a bearer token. Serve gives server-auth TLS + tailnet device-auth, terminating TLS on the server node (its ts.net private key lives on the box). Bearer tokens are stealable/replayable credentials; the change trades a client-cert factor for a bearer factor.
  FIX: Acknowledge the mTLS client-auth is being dropped and confirm bearer-token + tailnet ACLs + tailnet lock compensate. If any read path could ever be reached without TLS terminating at the trusted node, keep bearer tokens short-lived/rotatable (the design already rotates them freely) and bind them to device where possible.

notes: The wrapped-key custody core (G1) is sound and correctly modeled on restic/borg; the 'no escrow under the data key' property genuinely holds and recovery-after-Mac-loss works. The blockers are not in that core but in the composition around it: (1) syncing the master phrase through a cloud password manager makes every Mac a key-holder and contradicts the threat table's non-key-holder assumption while widening the attack surface the doc claims to avoid; (2) the redaction enforcement point is unpinned per tier and verbatim is a toggle rather than an origin-gated authorization, so remote egress can get raw plaintext. Both are fixable without redesign. The threat table is the artifact most in need of honest revision — it omits the highest-probability loss cases (unlocked/sleeping Mac, malware-a


=== SYNTHESIS ===

I treated the `Read` on `undefined` / "Today is `undefined`" as a broken template placeholder (no path was interpolated) — both red-team reviewers independently reached the same conclusion, and today's date is fixed as 2026-07-11 by the sizing cluster's own measurements. The four cluster designs plus their reviews are self-contained; the memo below is grounded in them.

---

# Engram Round-2 — Final Architecture Decision Memo

**Author:** Chief Architect · **Date:** 2026-07-11 · **Scope:** 19 registry items across clusters A (crypto/privacy), B (durability), C (search/mining), D (sizing/ops). N5 is unaddressed by any cluster and is carried as an open item for a round-3 owner.

The single most consequential ruling: **D1 selects a home mini on hardware you physically own → G4 tier (c) → the entire client-side key-management subsystem (G1's recovery phrase, HKDF, wrapped-key custody) is descoped from v1.** This collapses most of Cluster A's attack surface and both of its blockers.

## 1. Decision Table

| Item | Final recommendation (red-team fixes folded in) | Rationale | Conf | User input still required? |
|---|---|---|---|---|
| **G1** (key custody) | v1 keeps the **shipped server-held `ENGRAM_REMOTE_AT_REST_KEY` (scheme c)** — no client master key, no phrase, no HKDF. The restic/borg wrapped-key model becomes a **deferred spec gated on D1**; if it ever fires, the phrase is **strictly out-of-band with one-time manual per-machine entry (never cloud-synced)**, scrypt/Argon2id cost pinned, full 24-word phrase enforced, a **second wrapped copy under a passkey/hardware-key** for durability redundancy, and **restic/borg-as-a-tool evaluated before hand-rolling**. | Adopts both reviews' blockers: the full chain was v1 scope-creep for a mode that is off by default, and cloud-syncing the master phrase made every Mac a key-holder — contradicting G4's own threat table. Deferral removes the contradiction and the SPOF simultaneously. | High | Only if/when tier (a) fires: paper-vault vs hardware-key for the phrase. Rolls up into **D1**. |
| **G3** (redaction ordering) | Verbatim content-addressed store + **egress-time** redaction, but **enforcement point pinned per tier** (server-side under c, key-holder-side under a) and **`verbatim:true` is an origin-gated authorization, not a toggle: hard-denied for any non-local caller** (remote MCP / other tailnet node). Reframed as display hygiene, not a security boundary. | Resolves review blocker 2: under (c) the server decrypts, so a remote MCP client would bypass an app-only filter. Origin-gating replaces the dangerous "verbatim defaults on for remote?" question with a hard no. | High | None — origin gate settles it. Add a regression test that no remote read path returns unredacted bytes. |
| **G4** (encryption scheme) | **Tiered, (c) default** for owned hardware (shipped); **(a) Borg-model keyed-convergent only when a byte touches untrusted hardware** (VPS/cloud backup), and when it does, add a **per-repo secret `chunk_seed` (HKDF) driving the CDC chunker** and a **deterministic GCM nonce = KDF(chunk id)** (forbid any random-nonce path). Reject (b). Threat table rewritten honestly. | Reviews were right that (c) protects only powered-off theft and (a) leaks chunk-size fingerprints without a secret seed. But the honest residuals don't change the tier choice for a solo owner. | High | None directly — the tier is selected by **D1**. |
| **G5** (integrity + delete-gate) | Reject cksumvfs. CAS-key self-verify; **server recomputes SHA-256 on-disk before setting `backed_up_at`**; Mac-side scrub for local chunks + server-side scrub for server blobs, both with **"last successful scrub" persisted, surfaced, and alerted if overdue**. 6-condition gate recomputed at gate time; **local chunk eviction reuses gate conditions 2–5** and any GC is **reference-counted per content-hash across all manifests, never per-session**. | Adopts review blocker (no server-side re-verify before flipping durable) and the uncovered second deletion path (local eviction / dedup-aware GC). | High | Scrub cadence (weekly default) — preference. |
| **G2** (blob backup) | **`rclone copy` (additive-only, NOT `sync`) with `--checksum --immutable --b2-versions`** to an Object-Lock bucket, same bucket as Litestream. No restic/borg layering under scheme (c). | Blocker: `rclone sync` deletes destination objects absent at source — a local GC would issue delete-markers and silently erode the WORM backup the delete-gate trusts. `copy` + checksum closes both the destructive-default and the bit-rot-invisible-to-size-compare holes. | High | Provider/region + Object-Lock mode (governance/compliance) + retention ≥ GC window. |
| **N3** (version skew) | Versioned additive API (`/v1/…`) + `/capabilities` negotiation; **refuse to arm aging/delete when remote durability can't be proven**; server keeps prior major mounted for a deprecation window. | Not challenged by review. Refuse-to-delete-on-incompatibility makes skew durability-safe rather than silently destructive. | High | Deprecation-window length (short is fine for one owner). |
| **N4** (RPO/RTO + drill) | Metadata RPO ≤1s, blob RPO ≤24h but **irreplaceable-data RPO = 0** (delete-gate forbids deleting an original before its chunks are in a completed, verified backup); degraded RTO 0, rebuild ≤2h. Monthly automated drill **now adds a freshness assertion (heartbeat row vs wall-clock, FAIL if gap > 2× RPO)**, records **wall-clock duration**, and pushes PASS/FAIL via **PushNotification + a dead-man's-switch healthchecks ping**. | Adopts both majors: a drill that reassembles+rehashes but never checks currency will PASS on a silently-stalled Litestream stream (stale-but-consistent restore). | High | Drill cadence / sample N — preference. |
| **N6** (format evolution) | Three independent per-record tags (`algo` per chunk, `chunker_version` per session, `schema_version` per session); **lazy re-chunk only**; old/new coexist in the content-keyed store. | Both reviews endorsed this as correctly sized, not over-built. Format bumps are additive; zero forced re-upload. | High | None. |
| **G6** (server semantic) | **Defer.** v1 = FTS-only server mirror + local brute-force cosine over the resident corpus. Corrected latency band: **~40–120 ms/query at the 5-yr ceiling** (disk-backed vec0, not the in-memory 17 ms figure) — still trivially fine single-user. No ANN/pgvector/Qdrant/LanceDB. | Deferral survives the 2–4× latency correction. Server-side vectors would turn the encrypted blob store into a semantically-searchable copy of the archive — its one clean privacy property. | High | Is server-side semantic ever in v2 scope, or permanently rehydrate-on-open? |
| **N2** (federated search) | Local FTS is the always-**synchronously-returned** floor; server leg fires **only if a cached heartbeat flag says reachable** (a fresh connect can hang seconds over Tailscale/DERP), and its results **merge asynchronously**, local-wins, watermark-stamped. `offload_state` **flips synchronously in the rehydrate transaction**. **Scoped to single-primary-device querying for v1.** | Adopts the "800 ms isn't actually bounded" major by separating cached reachability from response timeout and never blocking the local answer on the server leg. | High | None load-bearing (timeout is a tunable default). |
| **N7** (decision mining) | **Heuristic-only v1**: `get_decisions`/timeline from the `insights` table + regex ("decided/chose/because", commit-linked). LLM mining stays **optional, lazy, on-demand, on the Mac, never on-ingest**; if wired, Claude Haiku 4.5 (~$20 to backfill 3,092 sessions) or Ollama for $0; cache keyed by `(session_id, content_hash, model, prompt_version)`. | On-ingest LLM mining couples a slow, failure-prone call to the archival hot path — forbidden. Heuristic v1 ships value at zero cost/latency. | High | Ship heuristic-only and stop, or wire the optional LLM pass — and if so, paid Haiku vs free Ollama? |
| **G7** (reclaim math) | Report **on-disk-verified** bytes, not DB `size_bytes`. Treat the **1,648 MB / 2,755-session already-missing-body gap as a P0**: ongoing, real data loss that justifies shipping Phase 1 now, independent of the reclaim debate. | Measurement on the live clone shows the reclaim number is small; the urgency is the ongoing loss, not the reclaim. | High | None. |
| **D1** (server sizing/ops) | **2 vCPU / 4 GB / 500 GB SSD primary + 300 GB backup.** Monitoring: healthchecks.io free tier + ntfy.sh push for the four checks (backup heartbeat, service-alive, disk >80 %, sync-lag >48 h) — **this is also the alert channel for Clusters B and C**. TCO ~0.5–1 hr/mo + 1 hr/yr drill. | 5-yr worst case (258 GB) is a commodity-SSD number. One monitoring substrate serves durability alerts, drill heartbeats, and N2's reachability flag. | High | **Which box + where the second backup lives — this is the load-bearing choice that selects the G1/G4 tier.** Committing the server OS also settles B's launchd-vs-systemd hedge. |
| **D2** (hot-window) | **Bounded rolling window, overruling round-1 "forever": skip/lite 90d, normal/premium 180d, + hard 15 GB local size-cap backstop (LRU-evicts oldest verified-remote bodies).** Eviction reuses G5 conditions 2–5. **In local-only mode the second durable copy is a direct Mac→offsite bucket `rclone copy`, not Time Machine** (removes the house-fire colocation risk). | "Forever" recreates the unbounded growth G7 measured (premium = 67 % of bytes, +1–2 GB/mo). Adopts durability review's TM-colocation blocker via the offsite-bucket route. | Medium | Cap size (15 GB) + window lengths — tunable, not load-bearing. |
| **D3** (exposure) | **Tailscale Serve only, no Funnel.** Require the **bearer token on every read path** (never treat tailnet membership as authz), plus **tailnet ACLs + tailnet lock (TKA)**. Migrating off nginx+private-CA to Serve's managed TLS is a **non-load-bearing cleanup, deferrable**; it drops mTLS for bearer, compensated by ACLs/TKA/short-lived tokens. | Adopts the "tailnet ≠ trust" and "membership ≠ authorization" majors. | High | Migrate off nginx now or defer as cleanup — preference. |
| **D4** (redaction depth) | High-precision **structured secrets only** (gitleaks/trufflehog regex + entropy: AWS/GitHub/OpenAI keys, PEM blocks, JWTs, connection strings). **No ML, no broad PII.** Explicitly **best-effort display hygiene with known gaps**, not a protection boundary. | False positives on code transcripts are catastrophic; the real secret boundary is encryption + key custody. | High | Add your own email / narrow PII to the ruleset, accepting FP risk? |
| **D5** (governance) | Archive-everything default + **ingest-stage per-project exclusion** (`archiveExclude` globs in settings.json). **Fail closed on missing/ambiguous cwd** (quarantine), and match globs against **any cwd a session touched**, not just its primary project. | Adopts the multi-cwd attribution minor: excluded NDA content must never reach the durable, replicated store. | High | Which projects/paths to exclude — pure content policy. |
| **N1** (multi-writer) | `machine_id` (Keychain UUID) namespaces server rows; content-hash dedup makes double-archive harmless; **settle-time debounce (2× stat ~5–10 s apart) before hashing**; flag same-session/different-hash as a **`sync_ledger` conflict**, never silent overwrite. No lock/CRDT. | True topology is one-writer-per-source-dir; the only real break is a cloud-synced source dir causing a torn-read corrupt CAS entry. | High | Is any source dir cloud-synced across two Macs? If yes, stop it (recommended) or accept detection-only guard. |
| **N8** (growth) | Plan a **5-yr band ~72–258 GB** (realistic-with-dedup floor → pessimistic ceiling), +25 % headroom for unscanned provider dirs (~6.3 GB already). | Measured run-rate 3.1–3.6 GB/mo; codex base64 resists compression (1.33×), plain JSON ~3× — hence a band, not a point. | Medium | None now; re-measure after Phase 3 base64 externalization. |

## 2. What the red-teams killed or changed

- **Descoped the entire v1 client-side key-management subsystem (G1).** Both reviews independently flagged it as scope-creep for an off-by-default mode and as internally contradictory (cloud-synced master phrase). v1 keeps only the shipped server-held key; the wrapped-key model is now a trigger-gated spec.
- **`rclone sync` → `rclone copy`.** The destructive-default deletion behavior would have silently eroded the WORM backup the whole delete-gate depends on. This is the single most dangerous line in the durability design.
- **Added server-side SHA-256 re-verification before `backed_up_at`.** Without it, a disk bit-flip in the 24 h pre-backup window becomes an immutable, corrupt, WORM-locked object — after which the delete-gate happily deletes the last good original.
- **Restore drill now checks freshness, not just reassembly.** A silently-stalled Litestream stream would have produced a stale-but-consistent PASS.
- **`verbatim:true` demoted from a settings toggle to an origin-gated authorization** (hard-denied for remote callers), and redaction enforcement pinned per tier. Redaction reframed everywhere as display hygiene, not a security boundary.
- **Local chunk eviction and any blob-store GC** are now explicitly bound by the same recompute-don't-trust gate and must be **reference-counted per content-hash**, not per-session, to preserve dedup safety.
- **"normal/premium forever" (round-1) overruled** by a bounded 180 d window + 15 GB cap; **local-only second copy moved from Time Machine to a direct offsite bucket** to kill the house-fire correlated-failure case.
- **All "alert" hand-waving wired to real channels** — PushNotification + healthchecks dead-man's-switch — reusing D1's monitoring substrate; "last scrub"/"last drill" both surfaced and overdue-alerted.
- **Corrected but survived:** sqlite-vec latency (40–120 ms, not 15–30 ms), N2 reachability bounding, threat-table honesty (added unlocked-Mac / malware-as-user / cloud-account rows). None changed a top-line decision.

## 3. Revised delivery-plan delta vs round-1

- **Phase 1 (ship now — P0, ongoing 1.65 GB loss):** durable CAS store + **scheme (c)** server-held encryption (already scaffolded) + `rclone copy` blob backup + Litestream metadata + **server-side verify-at-backup-boundary** + 6-condition delete-gate + heuristic `get_decisions` + FTS server mirror + monitoring wiring. The **restore drill moves INTO Phase 1** as a release-gate companion — you cannot arm deletion without a proven-recoverable, fresh backup.
- **Key-management spike DESCOPED from Phase 1** → a conditional "on-trigger" track that fires only if **D1** points backup at untrusted hardware, and starts by evaluating restic/borg-as-a-tool.
- **Phase 2 (local eviction):** D2 bounded window + 15 GB cap, reusing delete-gate conditions 2–5; local-only mode routes the second copy to the offsite bucket.
- **Phase 3 (compression / base64 externalization):** unchanged; re-measure N8 afterward.
- **Descoped from v1 entirely:** server-side semantic (G6 v2), pgvector/Qdrant/LanceDB, per-chunk random keys (G4-b), ML/broad-PII redaction, cksumvfs. **Deferred cleanup:** nginx+private-CA → Tailscale Serve.

## 4. Headline sizing numbers

- **Net local reclaim under the recommended D2 policy:** ~**1.1–1.2 GB** immediately (skip/lite >90 d + normal/premium >180 d), with local disk then **bounded at 15 GB** — the durable win is capping growth, not the one-time reclaim. Plus the **P0 finding: 1,648 MB across 2,755 sessions already lost** with no durable body anywhere.
- **Growth:** 3.1–3.6 GB/mo measured run-rate → **5-year band ~72–258 GB** (realistic-with-dedup → pessimistic no-compression), +25 % headroom.
- **Server floor:** **2 vCPU / 4 GB RAM / 500 GB SSD primary + 300 GB backup** — any existing 24/7 commodity box clears it; TCO ~0.5–1 hr/mo + 1 hr/yr drill.

**The one question that unblocks everything else:** *Where does the server live and where does its second backup copy sit?* — that answer selects the G1/G4 encryption tier and settles B's server-OS hedge. Everything else in the "user input" column is genuine preference (exclusion list, LLM-mining on/off, cap size, provider/region) and can be defaulted without risk.


=== CRITIC ===


-- minimal_core --
* G7/P0 — ship the durable CAS store now. The justification is not reclaim, it's ongoing loss: '1,648 MB across 2,755 sessions already lost with no durable body anywhere.' This is the whole point of v1; everything else is scaffolding around it.
* G4(c) — server-held encryption of blobs *before* they leave for the third-party bucket (already scaffolded). Not for 'powered-off theft' (the memo's honest residual) but because G2 ships the entire personal archive to a Backblaze B2 bucket; without encryption your whole archive sits in plaintext on a third party. Cheap, already built, keep.
* G2 — `rclone copy` NOT `sync`. SILENT SAFETY BREAK IF DEFERRED/WRONG: '`rclone sync` deletes destination objects absent at source — a local GC would issue delete-markers and silently erode the WORM backup the delete-gate trusts.' The single most dangerous line in the design; the copy-not-sync + `--checksum --immutable` choice is non-negotiable the moment blob backup exists.
* G5 — server recomputes SHA-256 on-disk before setting `backed_up_at`. SILENT SAFETY BREAK IF DEFERRED: 'a disk bit-flip in the 24h pre-backup window becomes an immutable, corrupt, WORM-locked object — after which the delete-gate happily deletes the last good original.' Verify-at-backup-boundary must gate durability, not the source-side hash.
* G3/D3 — bearer token on EVERY read path + `verbatim:true` as an origin-gated authorization 'hard-denied for any non-local caller.' SILENT SAFETY BREAK IF DEFERRED: under scheme (c) 'the server decrypts, so a remote MCP client would bypass an app-only filter' and 'never treat tailnet membership as authz.' The origin gate + the regression test that 'no remote read path returns unredacted bytes' are the actual auth boundary.
* D5 — 'Fail closed on missing/ambiguous cwd (quarantine)' + match exclusion globs against 'any cwd a session touched.' SILENT SAFETY BREAK IF DEFERRED: excluded NDA content that fails open reaches the durable, WORM-locked, replicated store and then cannot be deleted (Object-Lock is immutable). Fail-closed is irreversible-safety, must be in v1.
* N4 — restore drill WITH the freshness assertion ('heartbeat row vs wall-clock, FAIL if gap > 2× RPO'), wired as a release gate: 'you cannot arm deletion without a proven-recoverable, fresh backup.' SILENT SAFETY BREAK IF DEFERRED: reassemble-and-rehash alone 'will PASS on a silently-stalled Litestream stream (stale-but-consistent restore).' Cadence/sample-N are tunable; the freshness check is not.
* D1 — decide where the server + its second backup physically live. The memo's own headline: 'the one question that unblocks everything else' — it selects the G1/G4 encryption tier and settles the OS hedge. Not machinery, a decision that must be made before v1 is coherent. The 2vCPU/4GB/500GB floor is fine.
* D1 monitoring — the dead-man's-switch (healthchecks.io) ping. This is the one alert that fires on ABSENCE, so it's what actually catches a silently-stalled backup / overdue scrub / overdue drill. Absence-detection is a durability guarantee, not a nicety; a durability alert nobody receives is a silent break. (The paired ntfy/PushNotification is a cheap add, not itself load-bearing.)
* N6 (tags only) — stamp `algo` per chunk, `chunker_version` and `schema_version` per session FROM THE FIRST WRITE. Both reviews called this correctly sized. Writing the tags is near-free and is the cheap insurance that prevents corner-painting; the actual re-chunk work stays lazy/deferred.

-- defer --
* ALL deletion/reclaim — D2 local eviction and *arming* the 6-condition delete-gate. The safest useful v1 deletes nothing: it durably stores bodies and stops the P0 loss. The memo itself concedes reclaim is only '~1.1–1.2 GB' and 'the durable win is capping growth, not the one-time reclaim' — i.e. deletion buys ~1GB while carrying ALL the catastrophic risk. NON-SPLITTABLE CAVEAT: if/when eviction is armed (Phase 2), its whole chain is mandatory-core-together — the 6-condition gate recomputed at gate time, 'reference-counted per content-hash across all manifests, never per-session' (deferring this silently deletes chunks another session still references), plus the G5 verify and N4 drill-freshness. You may defer the feature; you may not partially defer its safety chain.
* G1 client-side key-management subsystem (recovery phrase, HKDF, wrapped-key custody). The memo already descopes it to a D1-gated 'on-trigger' spec — correct; keep it deferred, fired only if D1 points backup at untrusted hardware.
* N3 — versioned `/v1` API + `/capabilities` negotiation. Since deletion (the thing its 'refuse to arm aging/delete when remote durability can't be proven' interlock protects) is itself deferred, the whole item rides along. Keep just the refuse-to-delete-on-mismatch rule as a stub; build real API versioning when Phase 2 lands.
* G6 server-side semantic search — memo defers correctly. FTS-only server mirror + local brute-force cosine ('~40–120 ms/query' — 'trivially fine single-user') is enough; server vectors would forfeit 'the one clean privacy property' of the encrypted blob store.
* N2 federated-search server leg. Local FTS already exists and is 'the always-synchronously-returned floor' — that's the v1 need. The async server-merge / cached-heartbeat machinery is a deferrable enhancement; keep the guardrail 'never block the local answer on the server leg.'
* N7 decision mining — heuristic `get_decisions` is a low-cost feature, not a safety-or-usefulness floor for an archive; defer the whole thing but preserve the guardrail 'LLM mining never on-ingest' (coupling a slow failure-prone call to the archival hot path is forbidden).
* D4 structured-secret redaction (gitleaks/trufflehog regex). The memo reframes it as 'best-effort display hygiene with known gaps, not a protection boundary' — the real boundary is encryption + key custody. Cheap to add regex later; not a v1 blocker.
* D3 nginx+private-CA → Tailscale Serve migration — the memo's own words: 'non-load-bearing cleanup, deferrable.'
* N1 multi-writer machinery (settle-time debounce, sync_ledger conflict flag). The 'true topology is one-writer-per-source-dir'; the only real break requires a cloud-synced source dir, and the recommendation is 'stop that.' machine_id namespacing + content-hash dedup are cheap/harmless; the debounce only becomes core IF a source dir is actually cloud-synced across two Macs.
* N8 5-yr growth band + G7 reclaim accounting — informational sizing, no machinery; 're-measure after Phase 3 base64 externalization.' Feeds D1 but ships nothing.

-- gold_plating --
* G4-(a) hand-rolled keyed-convergent crypto — the 'per-repo secret `chunk_seed` (HKDF) driving the CDC chunker', 'deterministic GCM nonce = KDF(chunk id)', 'full 24-word phrase', 'scrypt/Argon2id cost pinned', and 'a second wrapped copy under a passkey/hardware-key.' Cost-vs-risk is upside-down for a solo owner in a mode that only fires on untrusted hardware and is off by default. SIMPLER SUBSTITUTE: the memo's own escape hatch — 'restic/borg-as-a-tool evaluated before hand-rolling.' Don't even *spec* hand-rolled convergent encryption; adopt borg (which already solves keyed-convergent chunking + nonce management) if/when tier (a) ever fires.
* Tailnet lock / TKA (D3). It defends against a rogue coordination server injecting an unauthorized node — a threat that doesn't exist for a solo operator who owns every node on a private tailnet. Machinery beyond the risk here. SIMPLER SUBSTITUTE: the bearer token on every read path (already core) + tailnet ACLs. Keep TKA on the someday-list, not v1.
* N3 deprecation-window machinery — 'server keeps prior major mounted for a deprecation window.' You control both ends (one server, one-few of your own Macs); there is nobody to be backward-compatible *for*. SIMPLER SUBSTITUTE: lockstep upgrade both ends together + the version-mismatch refuse-to-delete interlock. Skip the dual-major-mounted runtime.
* Already correctly CUT by the red-teams (affirming as avoided gold-plating, keep them cut): cksumvfs (G5); pgvector/Qdrant/LanceDB/ANN (G6); ML + broad-PII redaction (D4); per-chunk random keys G4-(b); lock/CRDT for multi-writer (N1). Each is machinery whose cost exceeded its risk-reduction for a single-user archive; the memo's rejections are right.

notes: "Read of `undefined` confirmed non-existent (working dir /Users/bing/-Code-/engram) — it is a broken template placeholder, matching the memo author's and both reviewers' conclusion; date 2026-07-11 stands. KEY REFACTORING the memo under-emphasizes: the safest useful v1 DELETES NOTHING. Durability (store + verified backup + drill) and reclaim (eviction + arming the delete-gate) are separable, and reclaim is worth only ~1.1–1.2 GB while carrying every catastrophic-loss risk in the design. Cleanest cut: ship durability-and-never-delete as v1 (minimal_core above), defer the entire delete/evict track — and treat its safety chain (6-condition gate, per-content-hash refcounted GC, server-side verify, drill-freshness) as an all-or-nothing bundle that cannot be partially shipped. SILENT-BREAK-IF-DEFERRED items pulled into minimal_core per instruction: G2 copy-not-sync, G5 server-side re-verify, G3/D3 origin-gated verbatim + bearer-on-every-read, D5 fail-closed-on-ambiguous-cwd, N4 drill-freshness-before-arming-delete. The memo's biggest correct move is already done — descoping the G1 client key-management subsystem; the remaining over-builds are the hand-rolled G4-(a) crypto (use borg), TKA, and N3's dual-major deprecation window."